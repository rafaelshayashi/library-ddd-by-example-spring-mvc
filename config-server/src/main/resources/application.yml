server:
  port: 8888
spring:
  profiles:
    active: git
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/Temp/config-repo
          search-paths: '{application}/{profile}'
        default-label: main